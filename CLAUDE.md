# CLAUDE.md

반려동물 훈련·문제행동 상담 RAG 시스템. 최종 목표는 안드로이드 앱이고, 지금은 API와
수집 파이프라인, 웹 채팅 화면까지 동작하는 단계다.

명령어·구조·수집 절차는 `README.md`에 있다. 이 파일은 **README에 없는 판단 규칙과
현재 위치**만 담는다.

## 현재 위치

- ✅ 데이터 수집 파이프라인 동작. **적재 기준 문서 294건 / 청크 11,354개**
  (PMC OA + ASPCA 수동분 + RSPCA 훈련 페이지), 네 축 전부, 커버리지 질문 8개 전부 PASS
- ✅ **검색까지 동작한다.** 청킹 → bge-m3 임베딩(1024차원) → pgvector 코사인 검색.
  `/chat`이 실제 근거 문서를 반환하고, 커버리지 질문 8개가 상위 5청크에서 근거를 찾는다
  (한국어 질문 → 영어 문서 교차언어 검색)
- ✅ Next.js 화면(`web/`) — **상담 채팅 UI**(말풍선 누적, 근거 접기, 멀티턴, 새 상담),
  ERD(`docs/erd.md`)
- ✅ **LLM 연결 완료.** `openai-compatible` provider. **현재 `.env`는 LM Studio
  로컬(`google/gemma-4-e2b`)을 가리킨다** — 19/20에 20초. Gemini 무료 티어
  (`gemini-3.1-flash-lite`)는 같은 기준에서 18/20에 5초이고 주석으로 남겨뒀다.
  **1점 차는 잡음이다** (아래 "로컬 LLM" 참조)
- ✅ **질의 재작성으로 커버리지 8/8.** 재작성 없으면 7/8 (기법 질문 1건 실패)
- ✅ **팩트체크** (`POST /api/v1/factcheck`, 화면 `/factcheck`). 유튜브·블로그를
  코퍼스에 안 넣기로 한 결정과 짝을 이룬다 — 오염원이 아니라 검증 대상으로 받는다
- ✅ **검색 품질 평가셋** (`scripts.eval.retrieval_report`, **21문항**). 기준선 10/20 →
  현재 **19/21** (covered 15/15, out-of-scope 4/4). 남은 실패 둘은 **진짜 코퍼스
  공백**이다 — 뛰어오르기, 그리고 억제된 배변(혼자 있을 때 참는 것. 반대 패턴인
  "혼자 있을 때 배변한다"는 35청크나 있는데 이쪽은 자료가 없다). 둘 다 전용 문서가
  없고 라이선스 깨끗한 소스를 못 찾았다 (판단 근거는 `sources.yaml`)
- ✅ **멀티턴 재현 검사** (`scripts.eval.replay`). 평가셋이 못 보는 층 —
  지시문 누출·이전 답변과의 겹침·라벨별 고정 문구·**답변 분량**을 본다
- ✅ **평가 판정 함수 자체의 테스트** (`tests/test_eval_judge.py`, 22개).
  평가셋이 거짓말을 하면 아무도 모른다 — `mentions()` 오탐으로 실제로 그랬다.
  DB·LLM 없이 순수 함수만 부른다
- ✅ **답변 형식 고정과 되묻기.** 훈련사 화법 + 폼 고정으로 1,200자 → 300자,
  마크다운은 서버에서 평문화(`plain_text.py`). 근거가 없어도 개 행동 질문이면
  거절 대신 원인을 가르는 질문을 되묻는다(`coverage=needs_detail`), 그 답을
  질의 재작성이 맥락으로 푼다(`history`)
- 다음 작업: 뛰어오르기 소스 확보 → Alembic → 안드로이드 앱

## 검색 품질에 대해 배운 것 (2026-08-12)

체감 품질이 나빠서("그냥 제미나이랑 대화하는 느낌") 추측 대신 측정한 결과:

- **코퍼스는 양이 아니라 주제 커버리지다.** 11 → 282건으로 늘렸는데 늘어난 266건이
  전부 논문이라 체감이 그대로였다. 보호자가 매일 묻는 배변·뛰어오르기·줄당김·리콜에
  전용 문서가 하나도 없었다. 문서가 있는 주제는 0.686~0.731, 없는 주제는 0.636~0.693
- **점수 임계값으로 "자료 없음"을 판별할 수 없다.** bge-m3의 한→영 코사인은 dynamic
  range가 좁아 분포가 겹친다(근거있음 최솟값 0.686 < 주제공백 최댓값 0.693).
  LLM에게 관련성을 묻는 쪽(`evidence_select.py`)이 실제로 동작했다
- **포기할 줄 아는 게 "RAG 느낌"이다.** 검색은 항상 top_k를 돌려주므로 근거 선별이
  없으면 코퍼스에 없는 주제에도 "가장 덜 무관한" 5건으로 답을 만든다.
  out-of-scope 통과율이 0/4 → 4/4로 바뀐 게 체감 차이의 대부분이다
- **평가셋 없이 코퍼스를 늘리지 말 것.** 무엇이 좋아졌는지 말할 수 없다

## 로컬 LLM에 대해 배운 것 (2026-08-13)

LM Studio + `google/gemma-4-e2b`(4.6B)로 바꿔보고 측정했다. 점수는 사실상 동률
(로컬 18~19/20 · Gemini 18/20, 1점은 잡음)이고 **실제 대가는 속도다 — 5초 → 14초.**
되돌리려면 `.env`의 Gemini 주석 세 줄을 살리고 `EMBEDDING_DEVICE=auto`.

- **추론형 모델은 짧은 호출을 조용히 죽인다.** 사고과정도 completion 토큰이라
  재작성(80)·근거 선별(60) 예산을 다 먹고 `content`가 빈 채로 잘린다. 두 단계 다
  폴백이 있어서 **에러 없이 비활성화**되고, 겉으로는 답이 잘 나오는데 범위 밖
  질문에도 근거가 붙는다. 원시 응답(`finish_reason`, `reasoning_tokens`)을 찍기
  전까지 원인을 못 찾았다. 지금은 그 상황에서 경고가 뜬다
- **작은 모델에는 한 호출에 한 가지만 시킨다.** 근거 선별이 "쓸 근거 고르기 +
  개 질문인지"를 같이 하고 있었는데 4.6B가 후자를 놓쳐 out-of-scope가 1/4였다.
  범위 판정을 단답 분류로 떼어내니 4/4로 돌아왔다 (18/20의 3점이 이것)
- **숙고가 필요한 곳은 판정뿐이다.** 답변 생성에 추론을 켜면 요청당 13초를 더 쓰는데
  평가 점수는 그대로다. 그래서 `LLMClient.generate`에 `reasoning`을 뒀다 — 호출부가
  "이 모델이 추론형이냐"가 아니라 **"이 일이 숙고를 요구하느냐"**를 말하게 한 것이다.
  질의 재작성도 같은 이유로 껐다 (6초 → 1초, 평가 점수 동일)
- **속도는 전부 "생성한 토큰 수"에 묶여 있다** (생성 25 tok/s, prefill 1,600토큰에 3초).
  그래서 `top_k`나 발췌 길이를 줄이는 건 효과가 거의 없다. 근거 선별의 8~12초는
  사고과정 250~300토큰의 값이고, **이건 줄이면 안 되는 비용이다** — 여기 추론을 끄면
  거절이 무너져 14/20으로 되돌아간다
- **LM Studio 로드 옵션으로는 더 못 얻는다. 다시 시도하지 말 것.** `--gpu max`는
  자동 판정이 이미 전체 오프로드라 무효, `--parallel 1`도 단일 요청엔 무의미,
  `--speculative-draft-mtp`는 이 GGUF에 MTP 헤드가 없어 불가하다
- **조언의 구체성이 다르다** — Gemini "켄넬 근처를 지날 때마다 간식을 안으로
  던져주세요" vs gemma "반려견의 반응을 관찰하세요". 이건 남아 있는 격차다
- ~~멀티턴 반복은 모델 한계다~~ — **틀린 결론이었다. 아래 "폼이 원인이었다" 참조**
- **평가셋이 답변 생성은 재지 못한다.** 검색·선별까지만 본다. 생성의 추론을 껐을 때
  폼 뒤에 사족이 붙는 건 점수에 안 잡혀서 답변을 직접 읽고서야 알았다
  (`trim_to_form`으로 잘라낸다)

## 반복의 원인은 폼이었다 — 모델 한계가 아니었다 (2026-08-13)

멀티턴 반복을 "gemma가 지시를 못 따른다"로 결론 내고 프롬프트 문구만 강화했다.
**인과가 거꾸로였다.** 폼이 질문 유형을 못 담아서 **채울 게 없으니 앞 답변을 복사한**
것이었다. 폼을 손대자 3번 겹침이 83% → 33%, 반복 경고가 1건 → 0건이 됐다.

- **답변 폼이 두 개뿐이었다** (문제행동 / 훈련법). "그러면 분리불안인건가?" 같은
  **판단형 질문**은 갈 곳이 없어 문제행동 폼으로 밀려 들어가고, 그러면 억지로 단계
  3개를 채워야 한다. 새로 할 말이 없으면 앞 답변을 그대로 가져온다
- **폼을 늘리지 않았다.** 질문 유형만큼 템플릿을 만들면 모델이 분류부터 틀린다.
  대신 `진단:`의 정의를 "질문에 대한 직접적인 답"으로 넓히고, 확인이 먼저인 질문이면
  가운데 라벨을 `이렇게 확인해요`로 바꿔 쓰게 했다. 라벨 하나를 고르는 것으로 충분했다
- **Gemini가 잘하던 것이 원인을 가렸다.** 같은 폼 미스매치에서도 Gemini는 새 내용을
  지어낼 여유가 있어 반복이 안 보였다. 그래서 "모델 차이"로 읽었는데, 실제로는
  **둘 다 겪는 설계 결함을 능력으로 가리고 있었다.** 모델 비교는 원인을 가르는 데
  유용하지만, "강한 모델이 통과한다 = 설계는 멀쩡하다"는 아니다
- **단정을 강요하는 규칙이 있었다** — "원인이 여러 갈래여도 가장 가능성 높은 하나로
  단정하고". 훈련사 화법을 만들려고 넣은 것인데 판단형 질문에서는 해롭다.
  "단정할 수 없으면 가능성으로 쓰고 무엇이 갈림길인지 붙이라"로 바꿨다
- **부분적으로만 아는 경우를 다루지 않고 있었다.** 지금까지 `coverage`는 전부 있음 /
  전부 없음뿐이었다. "덮는 부분만 답하고 나머지는 자료가 없다고 밝히라"를 규칙에 넣었다

## 검사가 헐거우면 고친 줄 안다 (2026-08-13)

멀티턴을 고치고 "축자 반복 0건"이라고 보고했는데 틀렸다. **재현 검사가 완전 일치만
봤기 때문**이다. 진단·주의점을 그대로 쓰고 단계 하나만 바꾼 답변이 통과했다 —
사용자 눈에는 같은 답인데.

- **부분 반복이 실사용에서 문제가 되는 형태다.** 문장 단위 겹침 비율로 본다
  (`scripts/eval/replay.py`). 임계 50%: 폼이 고정돼 라벨 줄은 항상 겹치므로 0이 될
  수 없고, 실제 문제 사례가 83~100%, 정상 후속 답변이 17% 아래였다
- **한 라벨이 매 턴 같은 말인지도 본다.** 4턴 중 주의점이 서로 다른 문장 2개면
  폼이 강제하는 자리를 관성으로 채우는 것이다. 총평이 아니라 필드 단위로 봐야 보인다
- **재현 대화는 사용자가 실제로 겪은 흐름만 넣는다** (`data/replay_conversations.yaml`).
  지어낸 대화는 실사용에서 무너지는 자리를 비껴간다

## 멀티턴은 평가셋이 못 잰다 (2026-08-13)

사용자가 6턴을 직접 나눠보고 대화를 가져왔다(밤 짖음 → 켄넬 → 분리불안). 20문항
평가셋은 **단발 질문의 검색·선별까지만** 보므로 여기서 나온 문제를 하나도 못 잡았다.
실사용 대화를 눈으로 읽는 절차가 평가셋과 별개로 필요하다.

- **같은 대화를 다른 모델로 재현하면 배관인지 모델인지 갈린다.** 고치기 전에 Gemini로
  같은 6턴을 돌렸더니 축자 반복은 **재현됐고**(→ 내 프롬프트 탓), 자기모순은 재현되지
  않았다(→ 4.6B 한계). 이걸 안 했으면 프롬프트만 만지며 원인을 못 찾았을 것이다
- **폼 슬롯에 라벨이 없으면 지시문이 그대로 복사된다.** 되묻기 폼의 마지막 줄이
  `<가능성 있는 원인들을 한 문장으로 나열합니다.>`였는데 그게 출력에 찍혔다.
  문제행동 폼은 `진단:`·`주의점:` 라벨이 슬롯을 붙잡아 준다. 라벨을 주니 사라졌다
- **작은 모델은 지시가 멀면 놓친다.** 반복 금지를 시스템 프롬프트에 넣었을 때
  gemma는 세 턴 연속 같은 답을 냈다(Gemini는 지켰다). 같은 문장을 **질문 바로 앞**으로
  옮기니 지켰다 (`RagService.FOLLOW_UP_HINT`)
- **그렇다고 항상 켜지는 자리에 넣으면 잡음이 된다.** 같은 지시를 선별의
  `SELECT_SYSTEM`에 넣었더니 **단발 질문 두 개가 깨졌다** (19/20 → 16/20). 대화가
  없는데 "이전 대화가 주어지면…"을 읽히면 판정이 흔들린다 — 초인종 질문은 검색
  결과가 완전히 같은데 판정만 뒤집혔다. **맥락이 있을 때만 붙인다**(`_CONTEXT_HINT`)
- **검색이 맥락을 쓰면 판정도 써야 한다.** 재작성에는 history를 넘기면서 근거 선별에는
  안 넘기고 있었다. "켄넬 훈련이 도움이 될까?"는 그 자체로 무엇에 대한 질문인지 알 수
  없어 되묻기로 떨어졌다. 맥락을 넘기니 답이 나온다

## 평가셋 자체가 틀릴 수 있다 (2026-08-13)

로컬 모델이 크레이트 질문에서 떨어져서 코퍼스를 뒤져보니 **자료가 있었다.**
ASPCA 분리불안 문서 안에 "To Crate or Not to Crate?" 단락이 통째로 있는데
`expect: uncovered`(거절해야 통과)로 라벨돼 있었다.

- **`uncovered`는 관찰이 아니라 주장이다** — "이 주제는 코퍼스에 없다". 코퍼스가
  자라면 그 주장이 낡는데, 라벨은 저절로 안 바뀐다. 어제 배변·리콜·줄당김을
  승격한 것과 같은 정비가 계속 필요하다
- **낡은 라벨은 틀린 동작을 정답으로 센다.** Gemini는 이 개 행동 질문을
  `none`(범위 밖)으로 처리하고 통과했다. 라벨 덕에 통과한 것이지 옳아서가 아니다
- **키워드가 판정을 무의미하게 만들 수 있다.** 크레이트에서 `den`이 sudden·evidence에
  걸리는 걸 보고 전체를 재봤더니 두 종류의 고장이 있었다 (11,354청크 기준):
  - **부분 문자열** — `tail`←detail, `aging`←managing, `cue`←rescue, `den`←sudden.
    `judge()`를 **단어 시작 경계**로 바꿔 해결했다(`retrieval_report.mentions`).
    앞쪽만 걸어야 한다 — 양쪽에 `\b`를 걸면 `bark`가 `barking`을 못 잡는다.
    이것만으로 `aging`이 669청크 → 342청크가 됐다(사라진 327건이 전부 managing)
  - **접두사 충돌은 이 방법으로 못 막는다** (2026-08-14 테스트로 확인). 여기 `lead`←leading을
    막힌 오탐으로 적어뒀었는데 **틀렸다** — `leading`은 `lead`로 시작하므로 앞쪽 경계가
    그대로 열려 있다. 막히는 건 `misleading`뿐이다. `bark`로 `barking`을 잡는 동작과
    같은 것이라 코드로는 가를 수 없고, **평가셋에서 두 단어로 쓰는 게 해법이다**
    (leash-pulling이 `loose lead`·`on the lead`를 쓴다). 지금 쓰이는 키워드 중
    피해자는 없다
  - **너무 흔한 단어** — `fear` 16.7%, `aggression` 13.6%, `stress` 10.5%,
    `anxiety` 9.4%. 주제를 안 다루는 근거도 통과시킨다. 결과어가 아니라
    주제어로 판정하게 바꿨다(처벌 질문은 `punishment`·`aversive`로 충분하다)
  - 정비 후 모든 키워드가 7% 미만이다. 새 항목을 넣을 때 이 기준으로 확인할 것
- **점수 ±1은 잡음이다.** 질의 재작성이 LLM이라 실행마다 문구가 달라지고 검색
  순위가 바뀐다. 같은 크레이트 질문이 한 번은 1위(0.732), 한 번은 커트라인 밖
  (0.650 vs 0.671)이었다. **총점을 비교하기 전에 어느 항목이 움직였는지 볼 것**
- **주로 흔들리는 항목은 리콜이다.** RSPCA 리콜 문서가 절반쯤 1위로 올라오고 나머지는
  ASPCA 공격성 문서에 밀린다. 평가셋의 옛 주석("논문에 밀려 상위에 못 온다")은
  `doc_type` 부스트 이전 것이라 지금은 절반만 맞다 — 주석도 데이터처럼 낡는다
- **"안정적"이라고 분류해둔 그룹도 흔들린다.** out-of-scope가 오래 4/4였는데 고양이
  모래가 한 번 실패했다(검색 점수는 0.506으로 네 번 다 동일한데 선별이 근거를
  0건 → 2건 남겼다). 다음 실행에 돌아왔다. **판정 LLM이 있는 한 어느 항목도 결정적이지
  않다** — 회귀를 의심하기 전에 코드 경로부터 확인할 것. 이 건은 `retrieval_report`가
  `RagService`를 아예 부르지 않아서 답변 프롬프트 변경과 무관함이 코드로 증명됐다
- **재작성이 해가 되는 경우도 있다.** 크레이트 질문에서 재작성이
  "desensitization and counterconditioning"을 붙이자 방법론 문서가 상위를 채우고
  정작 주제 문서를 밀어냈다. 어제 기법 질문을 살린 그 장치가 여기서는 반대로 작동한다

## 한국어 문서를 섞으면 순위가 통째로 뒤집힌다 (2026-08-17)

보듬TV 자막 199건(한국어)을 답변 코퍼스로 승격했다가 되돌렸다. **코퍼스를 늘렸는데
평가가 19/21 → 17/21로 내려갔다.** 원인은 자료의 품질이 아니라 **언어**였다.

- **1.7%의 청크가 상위 5위의 41%를 차지했다** (201/11,555 → 43/105). 24배다.
  고양이 모래·애견카페 같은 **범위 밖 질문에도 3/5가 한국어 문서**였다.
  09장에서 4/4이던 out-of-scope가 3/4으로 무너진 게 이것이다
- **길이 탓이 아니다.** 두 가설이 얽혀 있었다(한국어 청크 330자 vs 영어 1,061자).
  갈라보니 기존 영어 청크 중 짧은 것(<500자)은 **오히려 불리했다**(0.3배).
  길이가 원인이면 그쪽도 유리해야 하는데 아니다 → **언어가 원인이다**
- **bge-m3의 교차언어 검색은 되지만, 동일언어 쌍이 항상 이긴다.** 한국어 질문에
  한국어 문서가 있으면 주제가 안 맞아도 영어 문서를 밀어낸다. 뛰어오르기 질문의
  1위가 "이불에 **뛰어들어** 몸 비비는 강아지"였고, 억제된 배변 질문의 1위는
  "보호자가 없을 때는 벨소리에 **짖지 않는** 강아지"였다 — 표면 패턴만 맞았다
- **"자료가 늘었으니 좋아졌겠지"를 확인 없이 믿으면 안 된다.** 키워드로 세어보면
  뛰어오르기가 9건 잡혀서 공백이 메워진 것처럼 보였는데, 실제 근거를 읽으니
  주제가 달랐다. `mentions()` 오탐과 같은 함정이다 — **세지 말고 읽어야 한다**
- 되돌린 방법은 `documents.corpus`를 `observation`으로 바꾸는 것뿐이다.
  청크·임베딩은 그대로 두므로 승격/강등이 SQL 한 줄이다
### 풀었다 — 언어별 배경 유사도를 뺀다 (2026-08-17)

**"자기 언어의 배경보다 얼마나 튀는가"로 순위를 매기니 정리됐다.** 배경은 그 언어
청크 전체와의 평균 유사도다. 상위5의 한국어가 41% → **6%**(코퍼스 비율 1.7%),
평가는 두 번 돌려 **19/21·18/21**로 기준선과 동률인데 **실무 평점이 3.9·4.1로
기준선(3.7)보다 높다.** 통합 테스트도 전부 통과한다. 한국어를 넣고 손해가 없는
첫 설정이라 `corpus: answer`로 올렸다.

- **전체 스캔이 필요 없다.** 정규화된 벡터에서는 평균 유사도가 **평균 벡터와의
  내적과 정확히 같다**(실측 오차 0.000000). 언어별 중심 벡터 하나면 139ms → 2ms고
  코퍼스가 커져도 그대로다
- ⚠️ **코사인(`<=>`)이 아니라 내적을 쓸 것.** 코사인은 중심 벡터 크기로 나누는데
  단위 벡터를 평균 내면 길이가 짧아져 값이 부풀려진다 (0.396이어야 할 게 0.541).
  그리고 `func.avg`는 컬럼 타입을 잃어버리므로 `type_=Vector()`를 줘야 한다 —
  안 주면 벡터가 문자열로 돌아온다
- **z점수가 실패한 건 실수가 둘 겹쳐서였다.** (1) 표준편차로 **나눈 것**,
  (2) 기준을 **상위 후보**로 잡은 것. 상위 후보의 평균은 "얼마나 잘 맞았는지"에
  따라 움직이므로 빼면 살리려던 신호를 지운다. 둘 다 고치니 동작했다
- **고정 상수로 빼는 것도 기각했다**(`scripts.eval.language_offset`). 격차가 평균
  +0.059인데 표준편차 0.028로, 살려야 할 신호(0.04)만큼 흔들린다. 그런데
  **흔들리는 방향에 규칙이 있었다 — 범위 밖 질문 4개가 전부 격차 하위권**이다.
  이 값은 순수 언어차가 아니라 "질문이 그 코퍼스의 주제 영역인가"를 같이 담는다.
  그래서 상수가 아니라 질문마다 재는 게 맞다

### 그 전에 RRF를 붙여봤다 — 독식은 막았지만 균일한 세금이 됐다

언어별로 따로 뽑아 RRF로 합치는 걸 구현하고 재봤다(`rank_fused`). **총점은
18/21로 기준선과 같았고, 그게 문제다** — 문서 199건을 더 넣고도 본전이다.

- **out-of-scope가 4/4로 복구됐고** uncovered도 0/2 → 1/2로 나아졌다. 독식을
  막는다는 목적 자체는 달성했다
- **그런데 상위5의 한국어 비율이 41% → 42%로 그대로다.** 바뀐 건 분포다:
  전에는 질문마다 0/5~5/5(독식), 지금은 **전부 2/5로 균일**하다.
  고양이 모래·중성화 비용처럼 무관한 질문에도 2칸이 간다
- **청크 하나는 언어가 하나뿐이라 두 목록이 안 겹친다.** 그래서 RRF의 본래
  강점("여러 목록이 동의하면 올려준다")이 작동할 자리가 없고 **교대만 남는다.**
  하이브리드 검색에서 쓰던 도구를 다른 구조에 갖다 쓴 것이라, 같은 이름이어도
  같은 효과가 아니었다
- 대가가 두 군데서 잡혔다: 평가의 실무 평점 3.7 → 2.5, 그리고 **통합 테스트
  2개가 깨졌다**(기대하던 영어 근거가 상위 5위 밖으로 밀렸다). 테스트를
  느슨하게 고치지 않고 되돌렸다
- **z점수 표준화는 실패했다. 다시 시도하지 말 것.** 풀 안에서
  `(점수-평균)/표준편차`로 적응시키려 했는데 **밋밋한 풀일수록 표준편차가 작아
  나누면 하찮은 차이가 부풀려진다** (확 튀는 풀의 1등 z=2.00 vs 밋밋한 풀의
  1등 z=1.41). 고치려던 문제를 키운다. 단위 테스트로 계산해보고 알았다
- **다음 시도:** 오프셋은 질문의 성질이 아니라 **모델의 성질**이다. 질문마다
  다시 추정하지 말고, 여러 질문에서 한 번 재서 **고정 상수로 빼야** 한다.
  코드(`rank_fused`)는 남겨뒀다 — 코퍼스가 한 언어면 기존과 결과가 같다

## 리랭커도 판정기다 — 쓰기 전에 재라 (2026-08-17)

10장. cross-encoder 리랭킹을 붙였더니 hit@5가 **59.3% → 38.0%로 떨어졌다.**
모델이 약해서가 아니라 **아예 못 하는 일을 시킨 것**이었다.

- **명백한 쌍으로 30초 채점하니 바로 나왔다** (`BAAI/bge-reranker-base`):

      영어 질문 → 영어 정답      0.9972    ← 영어끼리는 완벽하다
      한국어 질문 → 영어 정답    0.0001    ← 교차언어를 아예 못 한다
      한국어 질문 → 한국어 정답  0.3658
      한국어 질문 → 한국어 오답  0.3555    ← 한국어끼리도 구분을 못 한다

  우리는 한국어 질문 + 영어 코퍼스다. 그래서 리랭킹이 관련도가 아니라
  **언어로 정렬**한다 — 같은 날 배경 보정으로 고친 병이 리랭커에서 재발했다
- **순서가 틀렸다.** 12장에서 "판정 LLM은 쓰기 전에 판정기를 재라"는 절차를
  만들어놓고, **리랭커도 판정기라는 걸 놓쳤다.** 30초 검사를 12분 실험 뒤에 했다
- **다국어 모델(`bge-reranker-v2-m3`, 2.3GB)은 이 PC에 안 들어간다.** VRAM은
  데스크톱 앱이 3.7GB를 써서 2.2GB만 남고 RAM도 3GB뿐이다. 실험 도구를
  retrieve/rerank 2단계로 나눠뒀으니 여유 있는 기기에서 **같은 후보 파일에**
  돌리면 재검색 없이 비교된다 (한 프로세스에 bge-m3와 cross-encoder를 같이
  올리면 segfault(139)로 죽는 문제도 이걸로 피한다)

### 진짜 수확은 실험 준비 중에 나왔다 — 청크 상한 2 → 3

검색 실패가 "맞는 문서인데 엉뚱한 청크"인 걸 보고 **문서당 청크 상한을 의심**했다.
정답이 그 문서의 세 번째 청크면 상한이 정답을 막고 있는 셈이다. 293문항 실측:

    상한 1   38.9%      상한 3   53.2%   ← 이득의 대부분
    상한 2   48.1%      상한 5   54.9%   ← 여기서 멈춘다

- **5가 아니라 3인 이유는 공짜가 아니어서다.** 문서 커버리지가 81.9% → 80.9%로
  떨어진다(근거가 한 출처로 쏠린다). 상한 5는 한 문서가 top_k를 다 채울 수 있어
  이 설정을 넣은 이유 자체가 사라진다
- 21문항 평가는 17/21 → **19/21**(covered 15/15), out-of-scope는 3/4이 나왔다가
  다음 실행에 4/4로 돌아왔다. **거절 능력은 제일 비싸게 얻은 것이라 5%p와 바꿀 수
  없어서 두 번 돌려 확인했다**
- **실험이 기각돼도 준비 과정의 관찰은 남는다.** 리랭킹을 안 해봤으면 상한을
  의심하지 않았을 것이다

## 이 PC의 함정 (2026-08-12 확인)

- **torch는 PyPI 기본 설치가 CPU 빌드다.** 처음에 이걸 모르고 적재를 5시간 돌렸다.
  실측으로 **GPU가 11배 빠르다** (11,281청크: CPU 163분 vs GPU 15분).
  `uv sync`가 torch를 다시 깔면 CPU 빌드로 되돌아가므로 그때마다 다시 설치할 것:
  `uv pip install --index-url https://download.pytorch.org/whl/cu126 --upgrade torch`
- **VRAM 6GB를 임베딩과 LLM이 나눠 쓴다.** 7B Q4(4.7GB) + bge-m3(2.3GB)는 안 들어간다.
  LM Studio를 켠 채 적재하려면 `EMBEDDING_DEVICE=cpu`, 적재만 할 거면 LM Studio를
  내리고 `auto`
- **uv도 Docker도 없었고 관리자 권한이 없다.** uv는 공식 스크립트로 설치했고,
  Docker 대신 `--extra pgdev`(pgserver 내장 Postgres)를 쓴다
- **HF Inference API는 무료로 못 쓴다** — 월 $0.10, `hf-inference`는 CPU 소형 모델만.
  Gemini 무료 티어(OpenAI 호환 엔드포인트)를 쓴다. 로컬이 필요하면 LM Studio로
  `LLM_BASE_URL`만 바꾸면 되고 코드 변경은 없다
- **PowerShell 5.1의 `Invoke-RestMethod`로 API를 확인하지 말 것.** charset 없는
  `application/json`을 ISO-8859-1로 디코딩해서 한글이 깨진 것처럼 보인다.
  서버는 정상 UTF-8이다. `curl`이나 python httpx로 확인할 것
- **API 키는 `.env`에만.** `.env.example`은 `.gitignore` 예외라 **커밋된다**

## 새 머신에서 시작할 때

수집 데이터(`data/raw/`, `data/processed/`), `.env`, DB는 저장소에 없다.
재수집·재적재가 정상 경로다. 전체 절차는 README의 "전체 파이프라인"에 있고,
수집만 확인하려면:

```bash
uv sync --extra collect
cp .env.example .env
uv run python -m scripts.collect.fetch --all
uv run python -m scripts.collect.normalize
uv run python -m scripts.collect.report   # 네 축 + 커버리지 8개 PASS면 정상
```

외부 스킬은 `.agents/skills/`로 커밋돼 있지만 **에이전트가 읽는 심볼릭 링크는
기기마다 다시 만들어야 한다** (이 저장소는 `core.symlinks=false`):

```bash
npx -y skills add kargnas/skills --skill git-lore --skill humanizer-kill-gpt --skill skill-manager --skill skill-prompter --skill vscode-ready
```

**`--skill '*'` 를 쓰지 말 것.** `ai-ready`가 딸려 오는데 "불필요한 환경변수면
`.env`를 제거하라"는 지시가 있고, 이 프로젝트의 `.env`는 git에 없어 되돌릴 수 없다.

**`uv sync`를 extra 없이 다시 돌리면 이전 extra가 제거된다.** 검색까지 쓰려면
항상 `uv sync --extra hf --extra collect`처럼 전부 나열할 것. 이걸로 두 번 헤맸다.

Docker나 관리자 권한이 없는 머신(학원 PC 등)은 `uv sync --extra pgdev` 후
`scripts.db.serve`가 내장 pgvector를 띄운다. 포트가 매번 바뀌어서 스크립트가
`.env`의 `DATABASE_URL`을 직접 갱신한다.

## 팀 규칙 (RAG·비서 파트 공통)

- uv / FastAPI / 모델은 자유 / **화면은 Next.js** / **DB는 각자 사용** + ERD 작성
- DB를 공용으로 쓰지 않는 이유는 서브 PC 적재 시 병목이다. 그래서 기기를 옮기면
  `scripts.db.init` + `load_corpus`를 다시 돌려야 한다
- 화면은 `web/`(Next.js)가 표준이다. `demo/streamlit_app.py`는 디버그용으로만 남겼다
- ERD는 `docs/erd.md` — **`app/db/models.py`를 고치면 같이 고칠 것**

## 기기를 옮겨 다닐 때

이 프로젝트는 학원 PC·집 PC·휴대폰을 오가며 작업한다. **로컬 세션은 다른 기기로
옮길 수 없다** — `claude --resume`은 그 머신의 로컬 기록만 본다. 반면 클라우드
세션은 어디서든 당겨올 수 있다.

- 시작: `claude --cloud "<할 일>"` — 클라우드 VM이 **GitHub 원격을 클론**한다
- 이어받기: `claude --teleport` (대화까지 그대로 터미널로)
- 폰에서 확인·지시: claude.ai/code 또는 Claude 모바일 앱

전제: 클라우드 세션은 커밋·푸시된 상태만 본다. 로컬에만 있는 변경은 보이지 않는다.
**작업을 마치면 반드시 커밋하고 푸시할 것.**

로컬 세션을 폰에서 이어 보려면 그 세션에서 `/remote-control`을 켜면 된다.

## 데일리 로그 (취몽 MCP)

세션을 마치거나 기능 하나를 완료하면 `submit_daily_log`로 그날 작업을 등록한다.
MCP 서버는 `chwijung`으로 등록돼 있다 (`claude mcp list`로 확인).

**올리기 전에 반드시 요약안을 보여주고 확인을 받는다.** 외부 서비스로 전송되고
포트폴리오·면접 자료로 쓰이므로 사용자가 내용을 보지 않은 채 나가면 안 된다.

무엇을 쓰는가 — 도구 설명이 요구하는 것이 **구현 디테일이 아니라 판단의 기록**이다:

- ✗ "`rag_service.py`의 `SYSTEM_PROMPT`를 수정하고 `plain_text.py`를 추가했다"
- ○ "답변이 논문 요약처럼 나와 실사용이 어려웠다. 원인을 프롬프트로 보고 화법과
  분량 규칙을 다시 정의했더니 1,200자 → 300자로 줄고 실행 가능한 조언이 됐다"

`problems` / `solution` / `result`는 필수이고 각 200자 제한이며, **잘리지 않고
그대로 포트폴리오에 실린다.** result는 가능하면 수치로 쓴다 — 이 프로젝트는
평가셋(`scripts.eval.retrieval_report`)이 있어서 "10/20 → 19/20" 같은 숫자를
바로 쓸 수 있다.

`content`(6000자)는 팀원이 보는 웹 화면용이고 마크다운이 HTML로 변환된다.
작은 작업이면 비워도 된다.

추측·과장 금지. 실제로 한 일만 쓴다. 안 끝난 것은 `status: in_progress`로 둔다.

## 데이터 수집 판단 규칙

이 프로젝트에서 소스 선정은 답변 품질을 직접 좌우한다. 유튜브·블로그를 배제하는
이유는 ToS 문제뿐 아니라 **지배이론 같은 폐기된 훈련법이 섞이기 때문**이다.
기관·학술 자료만 쓴다.

- **PMC OA 서브셋이 코퍼스 확대의 주력이다** (`fetcher: pmc`). 라이선스가 논문
  메타데이터(JATS `<permissions><license>`)로 같이 와서 읽을 ToS가 없고, 대부분
  CC-BY라 배포도 된다. 이걸로 11건 → 274건, `open` 267건이 됐다
- **PMC 질의는 반드시 `[tiab]`로 좁힐 것.** `canine separation anxiety`를 그냥 넣으면
  3,815건이 잡히고 상위 5건 중 4건이 무관 논문(치매 알림 시스템 등)이다. 같은 주제를
  `[tiab]`로 좁히면 22건이고 전부 관련 문헌이다. fetcher가 dog/canine 언급 3회 미만
  문서를 버리는 2차 방어도 있지만, 질의를 고치는 게 1차다
- **robots.txt 허용이 ToS 허용을 뜻하지 않는다.** 둘 다 확인해야 한다.
  ASPCA·MSD는 robots.txt가 전부 허용인데 ToS가 자동 수집을 금지한 실제 사례다
- **반대 방향도 있다** — robots.txt가 용도를 콕 집어 금지하는 경우. 네이버 블로그는
  `ClaudeBot`/`GPTBot`을 이름으로 막으면서 "BOT ACCESS FOR THE PURPOSES OF AI
  TRAINING AND RAG IS STRICTLY PROHIBITED"라고 적어뒀다. 이 프로젝트가 정확히
  RAG라서 수집하지 않는다. 판정 근거는 `sources.yaml` 맨 아래에 있다
- 새 소스는 `license: pending-check`로 넣는다 — `fetch`가 거부한다.
  `uv run python -m scripts.collect.check_license`로 확인 후 값을 갱신한다
- **판정 근거는 `data/sources.yaml`에 ToS 원문을 인용해 주석으로 남긴다.**
  같은 조사를 반복하지 않기 위해서다. 기존 항목들이 그 형식의 예다
- **배포 가능 여부는 `distribution` 한 필드로 본다** (`open` | `personal-only`).
  license 문자열을 매번 매칭하지 않으려고 적재 시점에 정규화한다 —
  `scripts/db/load_corpus.py:derive_distribution`. 모르는 값은 보수적으로
  `personal-only`다. 현재 274건 중 `open` 267 / `personal-only` 7
- **답변 근거 코퍼스와 관찰용 코퍼스는 물리적으로 분리한다** (`corpus` 필드).
  블로그처럼 지배이론이 섞일 수 있는 자료는 `observation`으로 넣고, 검색이
  `corpus = 'answer'`를 하드코딩으로 걸어 답변 근거에서 격리한다. 빈도 기반으로
  "훈련사들의 공통점"을 뽑으면 알파독·서열잡기가 상위로 오는데, 이는
  `avsab-dominance`가 정면으로 반박하는 내용이라 같은 풀에 있으면 답변이
  자기모순에 빠진다. `normalize`가 `corpus_blogs.jsonl`로 따로 뽑는다

## 코드 규칙

- **콘솔 출력에 `✓`·`⚠️` 같은 기호를 쓸 수 있는 건** `scripts/collect/__init__.py`가
  import 시점에 stdout/stderr을 UTF-8로 재설정하기 때문이다. 한글 Windows의 기본
  콘솔(cp949)에는 이 문자들이 없어 그것 없이는 `UnicodeEncodeError`로 죽는다.
  수집 패키지 밖에 새 CLI를 만든다면 같은 처리가 필요하다
- LLM·임베딩 구현체는 Protocol 뒤에 있고 **교체 지점은 각 `registry.py` 한 곳뿐이다.**
  `rag_service.py`와 엔드포인트는 건드리지 않는다 (절차는 README의 "Provider 교체")
- 모든 환경변수는 `app/core/config.py`에서만 읽는다
- **임베딩 모델은 lifespan에서 1회 로딩한다** (`app.state.embedder`). 요청마다 만들면
  수 GB 모델을 매번 읽는다. 로딩 실패는 앱을 죽이지 않고 검색 경로만 503으로 만든다 —
  torch 없이도 `/health`·`/docs`는 떠야 한다
- **`EMBEDDING_DIM`은 `chunks.embedding`의 `vector(N)`과 같아야 한다.** 모델을 바꾸면
  설정과 함께 `scripts.db.init --drop` + 재적재. 불일치하면 warmup이 즉시 죽는다
  (pgvector INSERT까지 가서 불친절하게 터지는 것보다 낫다)
- **검색의 `aversive` 제외와 `corpus='answer'` 필터는 하드코딩이다.** 코퍼스 품질
  불변식이지 호출자가 끌 수 있는 옵션이 아니다
- **권위 부스팅을 SQL `ORDER BY`에 넣지 말 것.** 조인 컬럼이 낀 표현식으로 정렬하면
  HNSW 인덱스를 못 써서 코퍼스가 커질 때 조용히 full scan이 된다. 과다 조회 후
  파이썬에서 재랭킹한다 (`vectorstore/ranking.py`)
- **대량 적재는 API가 아니라 `scripts.db.load_corpus`로 한다.** API 프로세스에 torch를
  상주시키지 않기 위해서다. 다만 같은 `IngestService`를 부르므로 구현은 하나다
- **LLM 판정에 `not_covered`(또는 동등한 "모름") 선택지를 반드시 넣을 것.** 빼면
  모델이 근거 없이 단정한다. 그리고 인용이 비었으면 **코드에서** 강등한다 —
  프롬프트로 부탁하지 않는다
- **한 호출에 한 가지만 시킨다.** 작은 모델은 판정 두 개를 묶으면 하나를 놓친다.
  프롬프트에 적혀 있어도 안 지킨다 (`evidence_select`의 범위 판정 분리가 그 사례)
- **폼을 지키게 하려면 프롬프트가 아니라 코드로 보장한다.** `trim_to_form`이
  정해진 구획 뒤의 사족을 잘라낸다. "반복하지 마세요"는 이미 프롬프트에 있었다
- **여러 주장을 검증할 때 LLM 구간만 `asyncio.gather`로 묶는다.** `AsyncSession`
  하나는 커넥션 하나라 DB를 병렬로 쓰면 깨진다. 검색은 수십 ms라 순차로 둬도 된다
- 주석과 커밋 메시지는 한국어로 쓴다. 무엇을 했는지보다 **왜 그렇게 했는지**를 남긴다
- 무거운 ML 패키지는 기본 설치에서 빠져 있다 (`uv sync --extra hf|demo|collect`)
